# Integration test targets

Open every scene, traverse the full navigation path and exercise multi-ball life loss, Phase Ball expiry and level completion. CI performs headless import/smoke parsing once Godot 4.7.1 is available.
