# Unit test targets

No third-party framework is bundled. Validate `SaveManager.defaults`, corrupted JSON recovery, score maxima, unlock idempotency, guest ID stability and Giant Paddle scale restoration. Exact manual cases are in `docs/MANUAL_TEST_CHECKLIST.md`.
